{
  "name": "natcrack",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "mkdir -p dist && cp index.html manifest.json sw.js favicon.svg icon.svg *.png dist/ 2>/dev/null || cp index.html dist/",
    "lint": "node -c server.js"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "katex": "^0.18.7"
  }
}
